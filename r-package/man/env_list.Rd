% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env.R
\name{env_list}
\alias{env_list}
\title{List variables in the active R environment}
\usage{
env_list(pattern = NULL)
}
\arguments{
\item{pattern}{Optional regex; only variables whose name matches are
returned. Default \code{NULL} (no filter).}
}
\value{
A list with one component \code{vars}, a list of records sorted
by name. Each record carries \code{name}, \code{type}, \code{class}, \code{length},
\code{size_bytes}, \code{description}, \code{is_data}.
}
\description{
Mirrors the MCP / CLI surface \code{env.list}. Returns one record per
variable visible in \code{globalenv()} (or in the environment currently
pinned in RStudio's Environment pane, if different), with the
metadata an agent typically wants: type, class, length, size in
bytes, and a short one-line description.
}
