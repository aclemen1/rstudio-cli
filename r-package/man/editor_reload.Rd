% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/editor.R
\name{editor_reload}
\alias{editor_reload}
\title{Reload a document from disk, replacing the live buffer}
\usage{
editor_reload(id, if_clean = FALSE)
}
\arguments{
\item{id}{Document id (length-1 character). Required: we don't pick
the active document implicitly because reloading a doc the user
isn't expecting would be surprising.}

\item{if_clean}{If \code{TRUE}, no-op when the buffer has unsaved
changes (instead of overwriting them). Default \code{FALSE}.}
}
\value{
\code{NULL} invisibly. Side-effect only.
}
\description{
Mirrors the MCP / CLI surface \code{editor.reload}. Useful when an
external tool (shell, git, your own write-to-disk) has updated a
file that's open in RStudio — without this call the user's buffer
would silently shadow the change until they manually reload.
}
\details{
The document id stays the same so cached references remain valid;
the dirty flag is cleared as a side-effect.
}
