% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{session_list}
\alias{session_list}
\title{List all active RStudio Server rsession sockets for the current user}
\usage{
session_list()
}
\value{
A list with one component \code{sessions}, a list of records
each carrying \code{socket} (the absolute path to the rsession Unix
domain socket).
}
\description{
Mirrors the MCP / CLI surface \code{session.list}. Scans
\verb{/var/run/rstudio-server/rstudio-rsession/} (or the path in the
\code{RS_SESSION_TMP_DIR} env var) for socket files. Useful when an
agent / CLI is running outside RStudio and needs to discover which
sessions it could connect to.
}
\details{
Empty list on platforms where the directory doesn't exist (Desktop
on macOS, Server with a non-standard config, etc.).
}
