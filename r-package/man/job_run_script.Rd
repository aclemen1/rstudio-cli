% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job.R
\name{job_run_script}
\alias{job_run_script}
\title{Run an R script as a background job}
\usage{
job_run_script(
  path,
  name = NULL,
  working_dir = NULL,
  encoding = "unknown",
  import_env = FALSE,
  export_env = ""
)
}
\arguments{
\item{path}{Path to the R script.}

\item{name}{Optional job name (defaults to the script's basename).}

\item{working_dir}{Optional working directory for the job (defaults
to the script's parent).}

\item{encoding}{Source encoding.}

\item{import_env}{If \code{TRUE}, copy the current global env into the
job process before running.}

\item{export_env}{Name of an env to receive the job's globalenv()
on completion (empty string = don't export).}
}
\value{
The new job id (character).
}
\description{
Wraps \code{\link[rstudioapi:jobRunScript]{rstudioapi::jobRunScript()}} — RStudio spawns a separate R
process, runs the script, and reports lifecycle back into the Jobs
pane. Unlike \code{\link[=job_add]{job_add()}}, this is "fire and forget" — RStudio
takes care of state transitions.
}
