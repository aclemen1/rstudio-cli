% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project_clone}
\alias{project_clone}
\title{Clone a git repository and add a \code{.Rproj} (destructive if open_after=TRUE)}
\usage{
project_clone(url, path = NULL, open_after = TRUE, new_session = FALSE)
}
\arguments{
\item{url}{Git repository URL (passed verbatim to \code{git clone}).}

\item{path}{Optional destination directory. When \code{NULL}, git's
default is used (the basename of the URL).}

\item{open_after, new_session}{See \code{\link[=project_new]{project_new()}}.}
}
\value{
Same shape as \code{\link[=project_new]{project_new()}} plus \code{git_initialized = TRUE}
(we cloned, so the result is necessarily a git repo).
}
\description{
Mirrors the MCP / CLI surface \code{project.clone}. \code{git clone <url>} into
\code{path} (or auto-derived from the URL), then write a \code{.Rproj} if the
cloned tree doesn't already carry one.
}
