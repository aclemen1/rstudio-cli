% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/editor.R
\name{editor_list}
\alias{editor_list}
\title{List every document currently open in the Source pane}
\usage{
editor_list()
}
\value{
A list with a single component \code{documents}, a list of
per-document records. Each record carries at least \code{id} and \code{path}
(path may be the empty string for an unsaved buffer).
}
\description{
RStudio doesn't expose an \code{rstudioapi} getter that enumerates open
documents; the catalog is shipped to the GWT client through
\code{client_init} (which we must NOT call). We scan the on-disk source
database directory (filenames matching \verb{^[0-9A-F]\{8\}$}) and pair each
id with whatever metadata \code{getSourceEditorContext(id = ...)} returns.
Mirrors the MCP / CLI surface \code{editor.list}.
}
