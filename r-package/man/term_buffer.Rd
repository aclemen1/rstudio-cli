% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term.R
\name{term_buffer}
\alias{term_buffer}
\title{Read the buffer of one terminal}
\usage{
term_buffer(id, strip_ansi = TRUE)
}
\arguments{
\item{id}{Terminal handle.}

\item{strip_ansi}{If \code{TRUE} (default), strip ANSI escape sequences;
if \code{FALSE}, keep them (raw OSC/CSI sequences come through).}
}
\value{
Character vector of lines.
}
\description{
Wraps \code{\link[rstudioapi:terminalBuffer]{rstudioapi::terminalBuffer()}}. Returns the lines as a
character vector. The CLI takes care of \code{tail()}-style truncation
before serialising — we don't truncate on this side so the function
is useful from R directly.
}
