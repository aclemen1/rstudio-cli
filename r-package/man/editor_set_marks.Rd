% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/editor.R
\name{editor_set_marks}
\alias{editor_set_marks}
\title{Show grep-style markers in the Markers pane}
\usage{
editor_set_marks(lines, name = "rstudio-cli", type = "info", base_path = NULL)
}
\arguments{
\item{lines}{Character vector of grep-format lines.}

\item{name}{Marker pane title (default \code{"rstudio-cli"}).}

\item{type}{Severity applied to every marker. One of \code{"info"},
\code{"warning"}, \code{"error"}. Default \code{"info"}.}

\item{base_path}{Optional base directory for resolving relative
\code{file} entries. Default \code{NULL} (let rstudioapi resolve).}
}
\value{
The list of markers passed to \code{sourceMarkers()} (invisibly).
}
\description{
Convenience wrapper around \code{\link[=pane_markers]{pane_markers()}} for the common case of
surfacing grep / ripgrep / ag output in the IDE. Accepts a character
vector of grep-format lines (\code{file:line:text} or \code{file:line:col:text})
and turns them into the \code{markers} argument
\code{\link[rstudioapi:sourceMarkers]{rstudioapi::sourceMarkers()}} expects. Lines that don't match the
grep pattern are silently skipped. Mirrors the MCP / CLI surface
\code{editor.set-marks}.
}
